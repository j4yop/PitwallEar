name: keep-alive

on:
  schedule:
    - cron: "*/7 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the live service to reduce cold starts
        run: curl -sf --max-time 120 https://pitwallear.onrender.com/health || true
