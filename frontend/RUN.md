# Frontend

```bash
cd frontend
npm install
npm run dev
```

The Vite dev server proxies `/demo`, `/analyse-text`, and `/analyse` to the
backend on `http://localhost:8000`.

Open `http://localhost:5173` and use the **Demo** tab first — it renders the
whole UI without any backend model downloads.
